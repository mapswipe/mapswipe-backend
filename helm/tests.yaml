tests:
  # operators.yaml (Dragonfly + redis env) first; traefik.yaml is layered after the
  # env overlay so its ingress class wins; tests/alpha.yaml (dummy image/host/secrets)
  # is auto-appended last by fugit.
  alpha.yaml:
    - values/operators.yaml
    - values/alpha.yaml
    - values/traefik.yaml
